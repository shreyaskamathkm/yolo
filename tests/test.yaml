hydra:
  searchpath:
    - file://yolo/config

name: test

defaults:
  - task: inference
  - dataset: mock
  - model: v9-s
  - general
  - _self_

task:
  backend: torch

model:
  compile:
    enabled: False
